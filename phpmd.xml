<?xml version="1.0"?>
<ruleset name="CMS Filament PHPMD ruleset"
         xmlns="http://pmd.sf.net/ruleset/1.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0 http://pmd.sf.net/ruleset_xml_schema.xsd"
         xsi:noNamespaceSchemaLocation="http://pmd.sf.net/ruleset_xml_schema.xsd">
    <description>Ruleset PHPMD para o package vagkaefer/cms-filament.</description>

    <rule ref="rulesets/cleancode.xml">
        <!-- Filament/Eloquent usam facades e boolean flags idiomaticamente -->
        <exclude name="StaticAccess"/>
        <exclude name="BooleanArgumentFlag"/>
        <exclude name="ElseExpression"/>
        <!-- Filament referencia classes totalmente qualificadas em closures/schemas -->
        <exclude name="MissingImport"/>
    </rule>

    <rule ref="rulesets/codesize.xml">
        <exclude name="ExcessiveClassLength"/>
        <exclude name="ExcessiveMethodLength"/>
        <exclude name="CyclomaticComplexity"/>
        <exclude name="NPathComplexity"/>
        <!-- Resources Filament agregam muitos componentes (alto acoplamento por design) -->
        <exclude name="TooManyPublicMethods"/>
    </rule>

    <rule ref="rulesets/design.xml">
        <!-- Resources Filament dependem de muitos componentes de UI -->
        <exclude name="CouplingBetweenObjects"/>
        <!-- O ModuleGeneratorService é um gerador de código, complexo por natureza -->
        <exclude name="ExcessiveClassComplexity"/>
    </rule>

    <rule ref="rulesets/naming.xml">
        <exclude name="ShortVariable"/>
        <exclude name="LongVariable"/>
    </rule>

    <rule ref="rulesets/unusedcode.xml">
        <!-- Assinaturas de interface do Filament/Laravel (canView($record),
             canAccessPanel($panel), Gate::before($user, $ability)) exigem
             parâmetros que nem sempre são usados. -->
        <exclude name="UnusedFormalParameter"/>
    </rule>
</ruleset>
